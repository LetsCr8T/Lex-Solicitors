import type { ReactNode } from "react";

export interface ContainerProps {
  children: ReactNode;
  className?: string;
}

/** STUB — centered max-width layout wrapper. Implemented in a later phase. */
export function Container() {
  return null;
}
