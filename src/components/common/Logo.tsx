export interface LogoProps {
  className?: string;
  /** Which surface the logo sits on, to pick the right color treatment. */
  variant?: "light" | "dark";
}

/** STUB — Lex Haven Solicitors wordmark / logo. Implemented in a later phase. */
export function Logo() {
  return null;
}
