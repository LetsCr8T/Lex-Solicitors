export interface SectionHeadingProps {
  /** Small label above the title. */
  eyebrow?: string;
  title: string;
  description?: string;
  align?: "left" | "center";
}

/** STUB — eyebrow + heading + description block. Implemented in a later phase. */
export function SectionHeading() {
  return null;
}
