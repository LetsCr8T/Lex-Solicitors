/**
 * Home page.
 *
 * Phase 1: an empty placeholder. Landing sections from
 * `@/components/landing/*` get composed here in a later phase.
 */
export default function Home() {
  return <main className="flex-1" />;
}
