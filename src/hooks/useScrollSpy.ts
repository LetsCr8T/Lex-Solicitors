/**
 * STUB — track which section is currently in view and return its id.
 * Becomes a Client Component hook (`"use client"`, IntersectionObserver) in a
 * later phase.
 */
export function useScrollSpy(): string | null {
  return null;
}
