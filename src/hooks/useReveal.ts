/**
 * STUB — reveal-on-scroll helper (respects `prefers-reduced-motion`).
 * Becomes a Client Component hook (`"use client"`, IntersectionObserver) in a
 * later phase.
 */
export function useReveal(): { isVisible: boolean } {
  return { isVisible: false };
}
