/**
 * STUB — subscribe to a CSS media query and return whether it matches.
 * Becomes a Client Component hook (`"use client"`, useState/useEffect) in a
 * later phase.
 */
export function useMediaQuery(): boolean {
  return false;
}
